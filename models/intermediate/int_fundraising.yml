models:
  - name: int_donations_enriched
    description: Donation records enriched with payment, campaign, and user context.
    columns:
      - name: donation_id
        tests:
          - unique
          - not_null

      - name: donation_status
        tests:
          - accepted_values:
              arguments:
                values: ['completed', 'failed', 'refunded']