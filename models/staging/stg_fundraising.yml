version: 2

models:
  - name: stg_donations
    description: Cleaned donation records from the raw donations table.
    columns:
      - name: donation_id
        tests:
          - unique
          - not_null

      - name: donation_status
        tests:
          - accepted_values:
              values: ['completed', 'failed', 'refunded']